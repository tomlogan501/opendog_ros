controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    back_test_legs_controller:
      type: position_controllers/JointGroupPositionController

joint_state_broadcaster:
  ros__parameters: {}

back_test_legs_controller:
  ros__parameters:
    joints: ["BL_uleg_joint", "BL_lleg_joint",
             "BR_uleg_joint", "BR_lleg_joint"]
