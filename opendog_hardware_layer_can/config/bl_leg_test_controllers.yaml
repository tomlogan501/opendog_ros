controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    bl_leg_test_controller:
      type: position_controllers/JointGroupPositionController

joint_state_broadcaster:
  ros__parameters: {}

bl_leg_test_controller:
  ros__parameters:
    joints: ["BL_uleg_joint", "BL_lleg_joint"]
